# Planet Journey Assets

`ChromaSource` preserves the original generated green-screen illustrations.
The production files in `Assets.xcassets` are derived with
`Tools/ink_art_pipeline.py --chroma-key --preset icon`.

The source screenshots supplied for UI inspiration are not source art and are
not included here.
